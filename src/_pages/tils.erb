---<%
{
	layout: :page,
	title: "Today I learned",
	description: "Snippets of what I'm learning",
	permalink: "/today-i-learned/"
}
%>---

<div class="mx-auto my-0 bg-white p-[20px] sm:p-[40px] pb-[24px]">
	<h2 class="mt-0">Today I learned</h2>
	<% collections.tils.each do |til| %>
		<div class="mb-[16px]">
			<div class="text-[10px] text-[#959595] font-medium"><%= til.data.date.to_date.strftime("%B %d, %Y") %></div>
			<div class="mt-0 mb-0">
				<a class="" href="<%= til.relative_url %>"><%= til.data.title %></a>
			</div>
		</div>
	<% end %>
</div>
