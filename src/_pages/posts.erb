---<%
{
	layout: :page,
	title: "Blog posts by Rachael Kalicun",
	description: "The thoughts and writing of Rachael Kalicun on topics like tech, travel, and personal development.",
	permalink: "/blog/"
}
%>---

<div class="mx-auto my-0 bg-white p-[20px] sm:p-[40px] pb-[24px]">
	<h2 class="mt-0">Writing</h2>
	<% collections.posts.each do |post| %>
		<div class="mb-10">
			<div class="text-[10px] text-[#959595] font-medium"><%= post.data.date.to_date.strftime("%B %d, %Y") %></div>
			<div class="mt-0 mb-0">
				<a class="" href="<%= post.relative_url %>"><%= post.data.title %></a>
				<p class=""><%= post.data.summary %></p>
			</div>
		</div>
	<% end %>
	
</div>
