---<%
{
	layout: :page,
	title: "Blog posts by Rachael Kalicun",
	description: "The thoughts and writing of Rachael Kalicun on topics like tech, travel, and personal development.",
	permalink: "/blog/"
}
%>---

<div class="mx-auto my-0 bg-white p-[20px] pb-[24px]">
	<h2 class="mt-0 alt_font text-3xl">My Writing</h2>
	<% collections.posts.each do |post| %>
		<div class="mb-10">
			<div class="post-date"><%= post.data.date.to_date.strftime("%B %d, %Y") %></div>
			<h3 class="post-title">
				<a href="<%= post.relative_url %>"><%= post.data.title %></a>
			</h3>
			<p><%= post.data.summary %></p>
		</div>
	<% end %>
	
</div>
