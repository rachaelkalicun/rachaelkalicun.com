---
layout: default
---
<div class="sm:w-[600px] mx-auto my-0 bg-white p-[40px] pb-1">
  <h1><%= data.title %></h1>
  <div class="text-xs"><%= data.date.to_date.strftime("%B %d, %Y") %></div>
  <%= yield %>
</div>
