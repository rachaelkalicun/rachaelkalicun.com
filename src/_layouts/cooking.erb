---
layout: default
---
<div class="mx-auto my-0 bg-white p-[20px] sm:p-[40px] pb-1">
  <div class="text-center mb-5">
    <h1 class="mb-0"><%= data.title %></h1>
    <div class="text-[10px] text-[#959595] font-medium"><%= data.date.to_date.strftime("%B %d, %Y") %></div>
  </div>
  <%= yield %>
</div>
